When running without DP (raw), the value is there and the trend is captured clearly.

When running with DP, accuracy improves as the mean gets higher; low means are affected more strongly by noise.

When testing with more groups, all groups need to be above the threshold, otherwise the query is rejected.

In general, the more people in the cohort, the better and more stable the result.
In testing, however all was inconclusive, as cohorts were too small.