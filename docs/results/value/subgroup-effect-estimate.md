no visible coarsening issues for this template

Ill keep proofs shorter from now on, as its all reproducible and does not bring more extensive value to them in unnecessary detail (eg different total cohort sizes, where most nodes reject bc size too small).

When running without DP this works perfectly.

When running with DP, when means are very small, the value of the data decreases.
Eg the two original results of means of two subgroups is 5.7 and 5.7; through DP it is then 6.9 and 3.2.
With bigger cohorts, the value stays more consistent.

Also when using a higher Epsilon, the value stays way more consistent.

In general, I would still argue that the value of the data is preserved.